# -*- mode: snippet -*-
# name: buck-target
# key: buck-target
# expand-env: ((package-name (file-name-base (directory-file-name (file-name-directory (buffer-file-name))))))
# --
fb_android_library(
    name = "`package-name`",
    srcs = glob(["*.java"]),
    deps = [$0
    ],
    visibility = [
        "PUBLIC",
    ],
)
